export enum AuthPathsEnum {
  LOGIN = "/login",
  NOT_FOUND = "/404",
  FORBIDDEN = "/403",
  REFUSED_CONNECTION = "/102",
}

export const AuthRoutePaths = {
  LOGIN: "/login",
};

export const AuthPaths = {
  LOGIN: () => "/login",
};
