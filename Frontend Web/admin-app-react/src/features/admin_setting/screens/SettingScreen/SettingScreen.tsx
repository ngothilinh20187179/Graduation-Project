import { Typography } from "antd";
import { memo } from "react";

const SettingScreen = () => {
  
  return (
    <div>
      <Typography>setting screen</Typography>
    </div>
  );
};

export default memo(SettingScreen);